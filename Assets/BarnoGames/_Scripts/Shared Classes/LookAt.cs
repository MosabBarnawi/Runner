using System.Collections.Generic;
using System.Collections;
using UnityEngine;

public class LookAt : MonoBehaviour
{
    //public float LookRotationSpeed;
    //private LivingEntity targetLivingEntity;
    //private bool canLookAt = false;
    ////private Transform Target;


    //#region Unity Callbacks
    //private void Start()
    //{
    //    //targetLivingEntity = GameManager.SharedInstance.PlayerInstance;

    //    if (targetLivingEntity != null)
    //    {
    //        canLookAt = true;
    //    }
    //}

    //private void Update()
    //{
    //    if (canLookAt)
    //    {
    //        if (!targetLivingEntity.isDead)
    //        {
    //            Transform Target = targetLivingEntity.transform;
    //            Vector3 lookPosition = new Vector3(Target.transform.position.x , transform.position.y , Target.transform.position.z);
    //            transform.LookAt(lookPosition);
    //            //transform.rotation = Quaternion.Lerp(transform.rotation , Target.rotation , Time.time * LookRotationSpeed);
    //        }
    //    }
    //}
    //#endregion
}
